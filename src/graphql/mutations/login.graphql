mutation Login($authinfo: UsernamePasswordInput!) {
  login(authinfo: $authinfo) {
    errors {
      field
      message
    }
    user {
      username
      createdAt
      updatedAt
      avatarId
    }
  }
}

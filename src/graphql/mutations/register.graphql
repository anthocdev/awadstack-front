mutation Register(
  $username: String!
  $password: String!
  $email: String!
  $avatarid: Int!
) {
  register(
    authinfo: { username: $username, password: $password, email: $email }
    avatarid: $avatarid
  ) {
    ...BasicUserResponse
  }
}

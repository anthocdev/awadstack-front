mutation UpdateComment($commentId: Int!, $body: String!) {
  updateComment(commentId: $commentId, input: { body: $body }) {
    updatedComment {
      id
      body
      likes
      dislikes
      updatedAt
    }
    error
  }
}
